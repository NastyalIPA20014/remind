# 🎯 Полное руководство по работе с XCode для iOS

## ⚡ Быстрый старт (5 минут)

```bash
# На Mac в терминале
cd /path/to/project/index-oblav-v6

# Запуск всей сборки сразу
chmod +x build-ios.sh
./build-ios.sh

# Откроется XCode автоматически
open ios/App/App.xcworkspace
```

Нажми **▶️ Run** (или `Cmd+R`) и готово! 🚀

---

## 📋 Пошаговое руководство для XCode

### Шаг 1: Подготовка проекта

```bash
cd ~/your-projects/remi/index-oblav-v6

# Установи зависимости
npm install

# Собери веб-версию
npm run build

# Синхронизируй с iOS
npx cap sync ios

# Установи CocoaPods
cd ios/App && pod install && cd ../..
```

---

### Шаг 2: Открой проект в XCode

```bash
# ВАЖНО! Открывай .xcworkspace, НЕ .xcodeproj
open ios/App/App.xcworkspace
```

**XCode откроется с таким видом:**

```
├─ App (проект)
│  ├─ App (основная папка кода)
│  │  ├─ AppDelegate.swift
│  │  ├─ public/              ← Веб-файлы (dist)
│  │  ├─ Assets.xcassets/     ← Иконки, изображения
│  │  └─ Base.lproj/
│  ├─ Pods (зависимости)
│  └─ App.xcworkspace
└─ Frameworks
```

---

### Шаг 3: Конфигурация в XCode

#### 3.1 Выбор цели (Target)

1. **Слева выбери:** `App` (проект)
2. **Вверху выбери:** `App` (target)
3. **Вверху в центре выбери устройство:**
   - **Для симулятора:** `iPhone 15 Pro` (или другая модель)
   - **Для реального iPhone:** Выбери свой iPhone (если подключен)

```
┌─ Product              ← Menu
│  └─ Scheme: App       ← Выбери здесь
│     └─ Destination: iPhone 15 Pro (Simulator)  ← И здесь
└─
```

#### 3.2 Signing & Capabilities (распиши приложение)

1. **Слева выбери:** `App` (проект)
2. **Сверху откройся вкладка:** `Signing & Capabilities`
3. **Настрой подпись:**
   - ✅ **Disable "Automatically manage signing"** (если требуется)
   - ✅ Или **"Automatically manage signing"** если у тебя есть Apple ID
   - Выбери свой **Team** (Apple Developer Account)
   - **Bundle Identifier:** `com.oblav.app` (уникальный ID)

**Если ошибка с подписью:**
- Убедись что у тебя есть Apple ID в XCode
  - XCode → Settings → Accounts → Apple ID
- Или отключи підпись (для симулятора не требуется)

#### 3.3 Проверка версии и сборки

1. **Сверху выбери:** `Build Settings`
2. **Найди эти значения:**
   - `Version Number (CFBundleShortVersionString)`: `6.0.0`
   - `Build Number (CFBundleVersion)`: `1`

---

### Шаг 4: Запуск приложения

#### Способ 1: Нажать кнопку Run
```
┌─────────────────────────────────────────┐
│  ▶️ Run    ■ Stop    ◀ ▶  ||  ⚙️ Options │  ← Нажми ▶️
└─────────────────────────────────────────┘
```

#### Способ 2: Горячая клавиша
```
Cmd + R  (на Mac)
```

#### Способ 3: Меню
```
Product → Run  (Cmd+R)
```

---

### Шаг 5: Процесс компиляции

XCode будет:
1. **Компилировать Swift код** (AppDelegate, etc) - ~30 сек
2. **Сборка бинарного файла** - ~30 сек
3. **Развёртывание на симулятор** - ~15 сек
4. **Запуск приложения** - ~5 сек

**Итого: 1-2 минуты в первый раз, потом быстрее**

---

### Шаг 6: Проверка в симуляторе

Когда приложение запустится, проверь:

✅ **Загрузка:**
- Видишь видео с "Индекс Облав"?
- Появляется главный экран?

✅ **Функциональность:**
- Нажимаешь на вкладки (Home, Chart, Map, News)?
- Загружаются ли данные?
- Работают ли кнопки?

✅ **Производительность:**
- Плавная ли анимация?
- Нет ли лага при переходах?

---

## 🔧 Решение проблем

### ❌ Ошибка: "Pod install failed"

**Решение:**
```bash
cd ios/App
rm -rf Pods Podfile.lock
pod update
pod install
cd ../..
```

### ❌ Ошибка: "Code signature invalid"

**Решение:**
1. XCode → Preferences → Accounts
2. Добавь свой Apple ID
3. Выбери Team в Signing & Capabilities

### ❌ Приложение не запускается на симуляторе

**Решение:**
```bash
# Перезагрузи симулятор
xcrun simctl erase all

# Установи заново зависимости
cd ios/App && pod install && cd ../..

# Пересобери в XCode
Cmd+Shift+K (Clean Build Folder)
Cmd+B (Build)
```

### ❌ "Cannot find app bundle"

**Решение:**
1. Убедись что открыл `.xcworkspace`, не `.xcodeproj`
2. Проверь что `public/` папка содержит `index.html`
3. Clean Build Folder (Cmd+Shift+K) и пересобери

---

## 📊 Структура файлов в Xcode

```
ios/App/App/
├─ AppDelegate.swift           ← Главный файл приложения
├─ SceneDelegate.swift         ← Делегат сцены
├─ ViewController.swift        ← Главный контроллер представления
├─ Assets.xcassets/            ← Иконки, цвета, изображения
│  ├─ AppIcon.appiconset/      ← Иконка приложения
│  └─ AccentColor.colorset/
├─ Base.lproj/                 ← Локализация (английский)
│  ├─ LaunchScreen.storyboard  ← Экран загрузки
│  └─ Main.storyboard          ← UI интерфейс
├─ public/                      ← ✅ ТВОЙ ВЕБ КОД (из dist)
│  ├─ index.html               ← Главная HTML страница
│  ├─ js/                       ← JavaScript бандлы
│  ├─ css/                      ← CSS файлы
│  └─ assets/                   ← Изображения, шрифты
└─ Info.plist                  ← Конфигурация приложения
```

---

## 🎨 Как изменить иконку приложения

1. Подготовь изображение (1024x1024 пикселей, PNG или JPG)
2. Откройся в XCode
3. **Assets → AppIcon.appiconset**
4. Перетащи твою иконку в нужный размер
5. XCode автоматически масштабирует

---

## 📱 Развёртывание на реальное устройство

1. **Подключи iPhone кабелем** к Mac
2. **Разблокируй iPhone**
3. **iPhone запросит "Trust This Computer?"** - нажми Trust
4. **В XCode вверху выбери:** твой iPhone (вместо симулятора)
5. **Нажми Run** (Cmd+R)
6. **XCode запросит код доступа** - разблокируй iPhone
7. **Приложение установится и запустится!** 🎉

---

## 🌐 Как сервер общается с фронтом

**На реальном устройстве:**
- iPhone подключен к WiFi
- App делает запросы на `http://localhost:3001` → **НЕ БУДЕТ РАБОТАТЬ**

**Решение - используй реальный IP сервера:**

### Вариант 1: Запусти сервер на Mac
```bash
npm run dev    # Запусти dev сервер (Vite + Express)
```

Узнай IP Mac:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
# Получишь что-то типа: inet 192.168.1.100
```

Edit конфиг в приложении (если нужно):
```javascript
// src/lib/api.js - измени на реальный IP
const API_URL = 'http://192.168.1.100:3001/api'
```

### Вариант 2: Используй ngrok для проксирования
```bash
npm install -g ngrok
ngrok http 3001  # Получишь публичный URL
```

---

## 📸 Горячие клавиши XCode

| Клавиша | Действие |
|---------|----------|
| `Cmd+R` | Run (запуск) |
| `Cmd+B` | Build (сборка) |
| `Cmd+Shift+K` | Clean Build Folder |
| `Cmd+K` | Clear Console |
| `Cmd+1` | Navigator |
| `Cmd+4` | Inspector |
| `Cmd+Shift+O` | Open Quickly (поиск файла) |

---

## 🚀 Отправка в App Store (будущее)

Когда приложение готово к публикации:

1. **Версия:** Увеличь номер версии в Info.plist
2. **Тестирование:** Протестируй на реальных устройствах
3. **Подписание:** Используй Distribution подпись
4. **Архив:** Product → Archive
5. **Загрузка:** Валидируй и отправляй через Transporter

---

## 📚 Полезные ссылки

- **Apple Developer:** https://developer.apple.com
- **XCode Documentation:** https://developer.apple.com/xcode
- **Swift Guide:** https://developer.apple.com/swift
- **Capacitor iOS:** https://capacitorjs.com/docs/ios

---

## ❓ Часто задаваемые вопросы

**Как симулировать медленный интернет?**
- Меню: Xcode → Open Developer Tool → Simulator
- В меню симулятора: Features → Network Link Conditioner

**Как посмотреть логи приложения?**
- Xcode → View → Debug Area → Show Console

**Как отключить режим Dark Mode для тестирования?**
- Меню симулятора: Features → Appearance → Light

---

**📝 Автор:** Индекс Облав v6  
**🔄 Последний обновл:** 2026  
**📞 Вопросы?** Смотри GitHub Issues  

---
