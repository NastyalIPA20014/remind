# 📱 iOS Компиляция - Полная Инструкция

## ✅ Что уже готово

- ✅ Веб-версия собрана (`npm run build`)
- ✅ Файлы скопированы в iOS проект
- ✅ Capacitor синхронизирован
- ✅ Podfile готов

---

## 🔨 Компиляция на Mac (локально)

### Шаг 1️⃣: Скачай проект на Mac

```bash
# На Mac, в терминале
git clone https://github.com/steisha-ops/-index.git
cd -index/index-oblav-v6
```

### Шаг 2️⃣: Установи зависимости

```bash
npm install
```

### Шаг 3️⃣: Собери веб-версию (если нужны изменения)

```bash
npm run build
```

### Шаг 4️⃣: Синхронизируй с iOS

```bash
npx cap sync ios
```

### Шаг 5️⃣: Установи Pod зависимости

```bash
cd ios/App
pod install
cd ../..
```

### Шаг 6️⃣: Открой в Xcode

```bash
# Команда для автооткрытия Xcode
open ios/App/App.xcworkspace

# ⚠️ ВАЖНО! Открывай .xcworkspace, а НЕ .xcodeproj
```

### Шаг 7️⃣: Конфигурация в Xcode

1. **Выбери проект слева** → `App`
2. **Вкладка "Signing & Capabilities"**:
   - ✅ Включи "Automatically manage signing"
   - Выбери свой Apple ID / Developer Team
   - Bundle Identifier: `com.oblav.app`

3. **Выбери target** (вверху):
   - Выбери iPhone симулятор или реальное устройство
   - Например: `iPhone 15 Pro` (симулятор) или твой iPhone (если подключен)

4. **Запусти приложение**:
   - Нажми **▶️ Run** (или `Cmd+R`)
   - Дождись компиляции (~1-2 минуты в первый раз)

---

## 📦 Структура проекта на iOS

```
ios/App/
├── App/
│   ├── App.xcodeproj/       ← Xcode проект
│   ├── App.xcworkspace/     ← Рабочее пространство (открывай это!)
│   ├── Podfile              ← Зависимости CocoaPods
│   ├── App/
│   │   ├── AppDelegate.swift
│   │   ├── public/          ← Веб-файлы (dist/...)
│   │   │   ├── index.html
│   │   │   ├── js/
│   │   │   ├── assets/
│   │   │   └── videos/      ← Твои видео (intro1, intro2, intro3)
│   │   └── ...
│   └── Podfile.lock
└── capacitor-cordova-ios-plugins/
```

---

## 🎮 После успешной компиляции

1. **Приложение откроется на симуляторе** (или на твоём iPhone если он подключан)
2. **Возможности:**
   - ✅ Увидишь интро с видеофоном
   - ✅ Все виджеты работают
   - ✅ Перетаскивание виджетов (нажми ☰ на главной)
   - ✅ API подключен к серверу
   - ✅ БД работает
   - ✅ Все анимации и переходы топ

---

## 🔧 Если что-то не работает

### Ошибка: "pod: command not found"
```bash
# Установи CocoaPods на Mac
sudo gem install cocoapods
pod setup
```

### Ошибка: "Cannot find module"
```bash
# Пересоздай симулятор
open ios/App/App.xcworkspace
# Xcode → Product → Clean Build Folder (Cmd+Shift+K)
# Xcode → Product → Build (Cmd+B)
```

### Видео не загружаются на iOS
**Убеди что в папке `/public/videos/` три файла:**
- intro1.mp4
- intro2.mp4
- intro3.mp4

Если их нет - закинь туда mp4 файлы.

### Белый экран при запуске
```bash
# Очистить кэш и пересоздать
cd ios/App
rm -rf Pods Podfile.lock
pod install
cd ../..
```

---

## 🚀 Быстрый запуск (для уже готового проекта)

Если всё уже настроено:

```bash
# На Mac
open ios/App/App.xcworkspace
# В Xcode: Cmd+R
```

---

## 📱 Тестирование на реальном iPhone

1. **Подключи iPhone кабелем** к Mac
2. **В Xcode** выбери свой iPhone вверху (вместо симулятора)
3. **Нажми ▶️ Run**
4. **На iPhone разреши установку** при первом запуске
5. **Готово!** Приложение загрузится на телефон

---

## 📊 Что внутри приложения

- **Главная** — Индекс Облав с виджетами
- **Герои** — Интервью с авторами контента
- **Карта** — Геолокация с маркерами
- **График** — Анализ данных по времени
- **Новости** — Лента с постами авторов
- **Отчет** — Форма для собственного репорта
- **История совести** — Трекер личного действия
- **Интро** — Трёхминутный видеоурок при первом запуске

---

## 📞 Данные сервера

По умолчанию приложение подключается к серверу на:
- **Local:** `http://localhost:3001`
- **Удалённо:** измени в `capacitor.config.json`

---

**Удачи!** 🎉 Если что-то не получается - пиши! 🚀
